# 2026-02-06 - Daily Notes

## City Fix Migration gestartet

**Problem:** 3.298.333 Hotels haben `city: "Unknown"` weil ETG-Dump keine separaten city/country_code Felder hat.

**Lösung:** Script `fix_hotel_cities.py` extrahiert Stadt aus `address.street` (letztes Segment nach Komma).

**Bug gefixed:** Script schrieb nach falschem Index `hotels` statt `hotels_de`/`hotels_en`.

**Status:** Läuft im Hintergrund
```bash
tail -f /tmp/fix_cities.log  # Progress checken
```

**Beispiele der Extraktion:**
- "Vlcie Hrdlo 1/A, Bratislava" → "Bratislava"
- "Mendeleev str., 150/4, Ufa" → "Ufa"
- "Via Pietro Nenni 22, Brescia" → "Brescia"

**Discovery:** `region_id: 0` für ALLE Hotels im ETG Dump → Region-Lookup unmöglich, Street-Parsing war einzige Option.

**Country_code Fix:** Aufgeschoben - bräuchte externe City→Country Mapping-Tabelle.

---

## Frontend ES Module Fix

**Problem:** `Cannot access 'eq' before initialization` Error in Safari
**Ursache:** Variable `let __lastSearchKey` war VOR imports deklariert (ES Module Temporal Dead Zone)
**Fix:** Moved nach imports in `SearchPageClient.tsx`

---

## CORS & SSE Fixes

- **CORS:** `ENVIRONMENT=development` für Tests (erlaubt alle Origins)
- **Nginx SSE:** `proxy_buffering off`, `proxy_cache off`, `proxy_read_timeout 300s`

---

## ETG API Compliance

9 Fixes committed: `9c45d10` "ETG API compliance fixes (from 8 API analyses)"
- Typed structs für alle API responses
- `popularity_score` mit `#[serde(default)]`
- Images Type: `Option<Vec<String>>` (match OpenSearch)
- Hash generation fixes
- Deprecated field annotations

---

## Performance Tuning

- **MAX_CONCURRENT_REQUESTS = 50** (32GB RAM erlaubt's)
- Dubai Search: 534 Hotels in 2 Sekunden

---

## Open Issues

1. **`eq` Initialization Error** - Noch nicht gelöst!
   - Error: `Cannot access 'eq' before initialization`
   - **NICHT in unserem Code** - aus bundled npm dependency
   - Problematic chunk: `.next/static/chunks/1949bdd2d71d6b2f.js`
   - Grep nach `eq` patterns war leer - Source minified
   - **Nächster Schritt:** Dependencies prüfen (date-fns, lodash, etc.) oder Source Maps

2. **City Fix Script** - Läuft noch (~1.5M Hotels remaining)
   - Session: `nimble-ocean` (PID 429127)
   - Check: `tail -f /tmp/fix_cities.log`

---

## CRITICAL LESSON LEARNED 🚨

**Falsches Verzeichnis editiert!**
- ❌ FALSCH: `/root/.openclaw/workspace/frontend-next`
- ✅ RICHTIG: `/root/ETG-Hotel-IBE/frontend-next`

Das Workspace-Verzeichnis hat NICHTS mit dem Projekt zu tun! Immer explizit `/root/ETG-Hotel-IBE/` verwenden.

---

## Rebuild durchgeführt

- `.next` folder gelöscht
- `npm run build` erfolgreich
- BUILD_ID: `ZWjl76Bk9v_yr26L3Zfxm`
- PM2: `pm2 restart hotel-frontend`
- **Chef muss Hard Refresh machen** (Ctrl+Shift+R / ⌘+Shift+R)

---

## City Fix Script ABGESCHLOSSEN (hotels_de)

**Ergebnis:** 1,526,146 Hotels korrigiert, 13,687 unverändert
**Aktuell laufend:** `hotels_en` (PID 463127)

---

## Language Switching Fixes (Nachmittag/Abend)

**Kernproblem:** Deutsches UI wenn Englisch ausgewählt

### Fixes implementiert:
1. **URL language priority:** `searchParams?.language || i18n.language || 'de'`
2. **React Query cache keys mit language:** Verhindert falsche Sprache aus Cache
3. **Static catalog reload:** Trackt `catalogLanguage` State
4. **German fallback strings:** Alle `t('key', 'fallback')` Fallbacks müssen Englisch sein
5. **Deposit "verfügbar" fix:** Skip availability für Deposits
6. **Meal type translations:** Overrides für alle Varianten
7. **Bed type normalization:** `normalizeBedTypeKey()` Funktion
8. **Translation functions:** Expliziter `language` Parameter nötig

### Key Decision:
**Translation functions müssen `language` Parameter erhalten** - `i18n.language` ist beim ersten Render nicht aktualisiert!

---

## Frontend Fixes (Abend)

- **PM2 Restart Loop gefixed:** Process recreated
- **Streaming API re-enabled:** War disabled, verursachte Redis Timeout bei großen Resultaten
- **CSP für analytics.swissbooker.ch:** Hinzugefügt
- **PulsingDots Component:** Für Loading States
- **Hardcoded "Non-smoking" entfernt:** War nicht übersetzt
- **HTML in Descriptions:** Escaped/stripped

---

## Rust Backend Search Analyse

**Potentieller Bottleneck gefunden:**
```rust
MAX_CONCURRENT_STREAM_REQUESTS: 4  // Sehr konservativ!
```

**Aktuelle Config:**
- `INITIAL_CHUNK_SIZE: 50`
- `REMAINING_CHUNK_SIZE: 100`
- `STREAMING_BATCH_SIZE: 300`
- Claims "ULTRA-FAST PARALLEL SEARCH" mit HTTP/2 multiplexing

**Context7 Research:** tokio async patterns (`tokio::join!`, `tokio::select!`, concurrent streams)

**Nächster Schritt:** `MAX_CONCURRENT_STREAM_REQUESTS` erhöhen auf 16-32 testen

---

## Console Errors (Non-blocking)

- TripAdvisor 404
- Similar Hotels 422
- Sitemap 404

---

## Open Tasks

1. [ ] Monitor hotels_en city fix completion
2. [x] Rust search optimization - DONE (siehe unten)
3. [ ] Test language switching on hotel detail page
4. [ ] Nice-to-have: Similar Hotels 422, TripAdvisor, Sitemap
5. [ ] ETG kontaktieren: Net Commission Model anfragen

---

## Rust Backend Search Optimierung (Mittag)

**Problem:** Batching war ineffizient - mehrere OpenSearch-Calls pro Suche

**Lösung implementiert:**
1. **INSTANT LOAD:** 1x ETG API → 1x OpenSearch bulk → ALLE Hotels auf einmal zurück
2. **Kein Batching mehr:** Vorher 3 Batches (100/500/rest), jetzt 1 Batch (alle)
3. **OpenSearch `_mget`:** Statt `_search` für ID-lookups (schneller)
4. **Parallel Chunks:** `futures::join_all` für große Mengen

**Performance nach Optimierung:**
- ETG API: ~20s (können wir nicht ändern)
- OpenSearch bulk: **168ms** (warm) für ~1000 Hotels
- Total Enrichment: **~200ms**
- Alle 32 Felder werden geladen

---

## ETG API Geschwindigkeitsanalyse

**Direkte curl Tests:**
| Endpoint | Zeit | Hotels | Limit |
|----------|------|--------|-------|
| `/serp/region/` | **23.7s** | 887 | 10/min |
| `/serp/geo/` | **20.4s** | 1859 | 10/min |
| `/serp/hotels/` (50 HIDs) | **2.3s** | 50 | 150/min |

**Fazit:** ETG's Server ist der Bottleneck, nicht unser Code.

---

## ETG Preismodell Discovery

**Problem:** Preise auf RateHawk Portal (1.923 CHF) ≠ unsere IBE (1.964 CHF)

**Ursache gefunden:** API liefert:
```json
{
  "amount_net": "1964.00",
  "amount_gross": "1964.00", 
  "amount_commission": "0.00"
}
```

**Erklärung:** Account ist im **Gross Model** konfiguriert:
- Gross Model: `amount_net = amount_gross`, keine separate Commission
- Net Commission Model: `amount_net < amount_gross`, Commission sichtbar

**Lösung:** ETG Account Manager kontaktieren:
> "Wir möchten auf Net Commission Model wechseln, damit amount_net den echten Einkaufspreis zeigt."

**Doku:** https://docs.emergingtravel.com/docs/b2b-api/hotel-search/retrieve-hotelpage/

---

## hotelpink.ch LIVE! 🎉

**Deployed um 11:19 UTC**
- Nginx von Maintenance auf Live-Proxy umgestellt
- SSL aktiv (Let's Encrypt)
- Frontend: https://hotelpink.ch → localhost:3001
- Backend API: https://hotelpink.ch/api/ → localhost:8081
- SSE Support konfiguriert

---

## hotelpink.ch UX Improvements (Nachmittag)

### FAQ Page Fix
- **Problem:** FAQ Seite hatte keinen Weg zurück zur Homepage
- **Lösung:** Layout-Wrapper hinzugefügt

### Nginx IPv6 Bug
- **Problem:** nginx nutzte `[::1]` (IPv6 localhost), aber Next.js nur auf `127.0.0.1`
- **Lösung:** Explizit `127.0.0.1` statt `localhost` in nginx config

### Nginx Performance
- **gzip Compression:** Alle MIME types aktiviert
- **Static Asset Caching:** `/_next/static/` mit 1-Jahr Cache-Control

### Legal Pages erstellt
- `/legal/agb` - Allgemeine Geschäftsbedingungen
- `/legal/impressum` - Impressum & Kontakt
- `/legal/datenschutz` - Datenschutzerklärung
- **Alle bilingual (DE/EN)** mit Language-Switcher
- **Keine tech stack Details** auf public-facing legal pages

### Footer Redesign
- Links zu Legal Pages hinzugefügt
- "Hotels suchen" entfernt
- **Smart Home Link:** Scrollt zu Search-Form auf Homepage, navigiert von anderen Seiten
- **Farbe:** `#1e2533` (zwischen gray-800 und gray-900)

### Account-Seiten Redesign
- **Profile:** Logged-in = Sidebar + Form, Logged-out = Landing mit Benefits
- **My Bookings:** Filter-Tabs, Hotel-Bilder, Voucher-Downloads
- **Favorites:** Grid/List Toggle, bessere Cards, Empty State

### Sitemap Fix
- Von `/sitemap-page` zu `/sitemap` umbenannt
- Layout-Wrapper hinzugefügt

### Text Contrast Fixes (Legal Pages)
- **Problem:** `text-primary-100` war zu dunkel auf Pink-Gradient
- **Lösung:** `text-white` und `text-white/70` für besseren Kontrast
- **Datenschutz:** Hero Card mit explizitem `text-white` und `text-white/90`

### Offen
- [ ] Impressum page evtl. gleiche white-text Fixes nötig
- [ ] Settings page Redesign (ähnlich Profile/Favorites)

---

## Key Technical Decisions

1. **IPv4 explicit in nginx:** Immer `127.0.0.1` statt `localhost` um IPv6-Probleme zu vermeiden
2. **Smart home link behavior:** `router.push('/')` + `setTimeout` scroll to form
3. **White text on pink gradients:** Opacity-based classes wie `text-primary-100` funktionieren nicht gut auf Gradients → explizit `text-white` nutzen
4. **Commission formula:** `net_amount * (1 + commission_rate)` - aber Account ist im Gross Model, daher aktuell kein Aufschlag sichtbar

---

## Booking Page UX Polish (Abend)

### Translations hinzugefügt
- `payment.noFee`, `payment.fee`, `payment.subtotal`, `payment.totalWithFee`, `payment.sslSecure`
- "Reisende" → "Gäste" (additionalTravelers → additionalGuests)

### Terms/Privacy Links gefixed
- Zeigen jetzt auf `/legal/agb?language=XX` und `/legal/datenschutz?language=XX`

### Country Dropdown gefixed
- Zeigt "Switzerland" statt "CH"
- Nutzt `language` Variable statt `i18n.language`

### Salutation Dropdowns gefixed
- Herr/Frau (DE) oder Mr/Mrs (EN) korrekt
- **Keine "Diverse" Option** - nur Mr/Mrs
- Schmalere Breite (w-28)

### Form Input Icons entfernt
- `autoComplete="off"` verhindert Browser Autofill Icons

### Booking Form Layout
- `grid-cols-[120px_1fr_1fr]` für Name-Felder
- `max-w-xl` Container für Konsistenz
- `max-w-5xl` für Hauptcontainer (schmaler)

### Hero Text geändert
- "millions of travelers" → "thousands of satisfied guests"

### Footer Copyright Kontrast
- `text-white/70`, Links `text-white/90`

### Profile Page Fixes
- Icons aus Inputs entfernt
- Native `<select>` statt Radix Select (funktioniert besser mit i18n)
- `useState` für `currentLanguage`, Updates nach Hydration
- Dropdown Widths: Salutation w-28, Country w-48
- Explizites `text-white` auf Pink-Gradient Header

### Booking Pre-fill from User Profile
- Alle Felder: salutation, phone, address, postalCode, city, country
- Erster Room Main Guest wird auch befüllt

### Booking Summary Improvements
- Stars jetzt unter Hotel-Name (nicht daneben)
- Volle Adresse angezeigt (nicht nur Stadt)
- Payment Fee Updates Total dynamisch (TWINT/Reka)
- Reka Logo vergrößert (100px statt 70px)

### "Select" Placeholder gekürzt
- EN: "Select", DE: "Auswählen"

---

## Key Decisions (Booking)

1. **Native `<select>` für i18n:** Radix Select rendert Children nicht dynamisch → native `<select>`
2. **Kein Parallel Prebook:** Chef lehnte ab - 11s Dialog ist akzeptabel
3. **Payment Fee Callback:** `onPaymentMethodChange` notifies Parent für Summary Updates

---

## Istanbul Search Issues (Open)

**Problem 1:** Zu wenige Hotels - 507 statt 1000+ erwartet
- OpenSearch: 566 Hotels gefunden
- Nach ETG Enrichment: 507 (einige haben keine Preise)

**Problem 2:** Error Message bei Parameter-Änderung
- "Error loading" erscheint in SearchPageClient.tsx Zeile 1263
- Noch nicht debugged

**Wichtig:** Search nutzt `region_id=1639`, nicht City-Name "Istanbul"
- City Fix Script hat KEINEN Einfluss auf Search-Ergebnisse!

---

## Multi-Geo Search für große Städte (Abend)

**Problem:** Istanbul Region-Search lieferte nur ~500 Hotels statt 1700+

**Ursache:** ETG Region API hat Limits, Geo API liefert mehr

**Lösung implementiert (Commit `88fe220`):**
1. **Threshold-Check:** Regions mit 3000+ Hotels in OpenSearch → Multi-Geo aktiviert
2. **3x3 Grid-Suche:** 9 überlappende Geo-Queries statt 1 Region-Query
3. **Deduplizierung:** Merged results by `hotel_id`
4. **Performance:** Parallel execution mit `tokio::join!`

**Konstanten in `constants.rs`:**
```rust
MULTI_GEO_GRID_SIZE: 3
MULTI_GEO_RADIUS_KM: 20.0
MULTI_GEO_STEP_DEGREES: 0.15
MULTI_GEO_HOTEL_THRESHOLD: 3000
```

**Test-Ergebnisse Istanbul:**
| Source | Hotels |
|--------|--------|
| Region API | ~540 |
| Geo Center | 1739 |
| Geo NW | 1718 |
| Geo NE | 1743 |
| **Multi-Geo Total** | **1700+** |

---

## Saferpay Payment Methods Fix (Abend)

**Problem:** Kreditkarten-Zahlung funktionierte nicht

**Ursache:** Falsche JSON-Struktur
```rust
// ❌ FALSCH
PaymentPageOptions { payment_methods: Some(PaymentMethodsOptions { allow: methods }) }

// ✅ RICHTIG
PaymentMethods: ["VISA", "MASTERCARD", ...] // Top-level Array!
```

**Fix (Commit `c4b2be8`):**
- `PaymentMethods` als top-level `Option<Vec<String>>`
- `Wallets` als separate top-level `Option<Vec<String>>`
- Wallets (ApplePay, GooglePay) werden bei Kreditkarte mit aktiviert

---

## Payment Flow Token Bug (Abend)

**Problem:** "No payment token found" auf Return-Page

**Ursache gefunden:**
- `PaymentSection.tsx` speichert `pendingBooking.paymentToken`
- `return/page.tsx` suchte nach `saferpayToken` (falscher Key)

**Fix:** Return page extrahiert Token jetzt korrekt aus `pendingBooking.paymentToken`

**PendingBooking Struktur (vollständig):**
```typescript
{
  paymentToken: string,
  bookingRooms: RoomBooking[],
  selectedPayment: PaymentMethod,
  formData: BookingFormData,
  hotel: HotelDetails,
  rate: RateDetails,
  searchParams: SearchParams,
  prebookData: { book_hash, hotel_name, ... }
}
```

---

## Breadcrumb Navigation Fix (Commit `76eccd3`)

**Problem:** Breadcrumb Region-Click navigierte nicht korrekt

**Ursache:** `regionId` war String aus API, aber Navigation erwartete Number

**Fix:** `parseInt(regionId)` bei Storage

---

## Still Running

- **hotels_en city fix:** PID 463127, check `tail -f /tmp/fix_cities.log`

---

## Open Tasks

1. [ ] Monitor hotels_en city fix completion
2. [ ] **Payment flow complete testing** - verify full booking works
3. [ ] **ETG booking API integration** - `partner_order_id`, `rooms[].guests[]`, HTTPS `return_path`
4. [ ] Determine actual commission_rate (expected 15%, observed ~2.1%)
5. [ ] Settings page redesign
