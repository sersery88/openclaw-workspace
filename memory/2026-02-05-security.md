# Security Hardening - 2026-02-05

## Durchgeführte Maßnahmen

### UFW Firewall ✅
- Unnötige Regeln entfernt (Port 3000, 8080)
- Erlaubte Ports: 22 (SSH), 80 (HTTP), 443 (HTTPS), 8082 (Hotel IBE Test)
- Default: deny incoming, allow outgoing
- OpenClaw Gateway (18789) nur localhost

### Postfix SMTP ✅
- `inet_interfaces` von `all` auf `loopback-only` geändert
- War vorher auf 0.0.0.0:25 offen → Spam-Relay-Risiko behoben
- Backup: `/etc/postfix/main.cf.bak.20260205`

### OpenClaw Security ✅
- Credentials-Ordner: chmod 700 (war 755)
- Telegram DM Policy: `open` → `allowlist` (nur 577361584)
- DM Scope: `per-channel-peer` (Session-Isolation)
- Security Audit: 0 critical, 1 warn (trusted_proxies - nur relevant bei Reverse Proxy)

### Nginx SSL ✅
- `ssl_prefer_server_ciphers` auf `on` gesetzt (TLS 1.2 Hardening)

### Lynis Audit ✅
- **Hardening Index: 76/100**
- **0 Warnings**
- **17 Suggestions** (meist nice-to-have wie GRUB Passwort, separate Partitionen)
- Tests durchgeführt: 261

## Bestehende Sicherheit (war schon da)
- fail2ban ✅ aktiv
- SSH Key-only Auth ✅ (PermitRootLogin prohibit-password)
- unattended-upgrades ✅ aktiv
- auditd ✅ aktiv
- rkhunter ✅ installiert
- TLS 1.2/1.3 only ✅

## Offene Suggestions (Low Priority)
- GRUB Boot-Passwort setzen
- umask auf 027 statt 022
- /home und /var auf separate Partitionen
- File Integrity Tool (AIDE/Tripwire) installieren
- Compiler-Zugang einschränken
- External Logging einrichten
